/**
 * Bump whenever the private CLI-facing MCP input/output contract changes.
 * It is advertised in serverInfo.title so same-semver development daemons
 * cannot be mistaken for the currently bundled CLI implementation.
 */
export const ZVEC_GREP_CLI_CONTRACT_ID = "zvec-grep-cli-contract-v1";
